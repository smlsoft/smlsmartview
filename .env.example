SESSION_SECRET=
SESSION_COOKIE_SECURE=false

# DATABASE_URL is no longer used — configure via DB_* only
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# AI Assistant Embedded Target
NEXT_PUBLIC_AICHAT_URL=http://localhost:3010

# Login page defaults (server-rendered at runtime; fill in the real values before deploying)
LOGIN_DEFAULT_PROVIDER_CODE=<รหัสผู้ให้บริการ ตอนเข้าหน้าโปรแกรม SML>
LOGIN_PREFERRED_DB_CODE=<รหัส db ตัวจริงของ production>
